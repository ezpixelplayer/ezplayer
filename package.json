{
    "name": "@ezplayer/ezplayer",
    "version": "0.5.3",
    "private": true,
    "type": "module",
    "repository": {
        "type": "git",
        "url": "https://github.com/ezpixelplayer/ezplayer.git"
    },
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "lint-staged": {
        "*.{ts,tsx,js,jsx,json,css,md}": "prettier --write"
    },
    "scripts": {
        "build": "pnpm --recursive --workspace-root=false build",
        "postbuild": "pnpm run pack:shared && pnpm run pack:player && pnpm run pack:core && pnpm run pack:epp",
        "build:win": "pnpm --filter './packages/*' build && cd apps/ezplayer-ui-electron && pnpm build:react && pnpm build:tsc && pnpm build:preload && pnpm electron-builder --win",
        "build:mac": "pnpm --filter './packages/*' build && cd apps/ezplayer-ui-electron && pnpm build:react && pnpm build:tsc && pnpm build:preload && pnpm electron-builder --mac",
        "build:linux": "pnpm --filter './packages/*' build && cd apps/ezplayer-ui-electron && pnpm build:react && pnpm build:tsc && pnpm build:preload && pnpm electron-builder --linux",
        "pack:shared": "cd packages/shared-ui-components && rm -rf dist/*.tgz && pnpm pack --pack-destination ./dist",
        "pack:player": "cd packages/player-ui-components && rm -rf dist/*.tgz && pnpm pack --pack-destination ./dist",
        "pack:core": "cd packages/ezplayer-core && rm -rf dist/*.tgz && pnpm pack --pack-destination ./dist",
        "pack:epp": "cd packages/epp && rm -rf dist/*.tgz && pnpm pack --pack-destination ./dist",
        "lint": "pnpm --recursive --workspace-root=false lint",
        "format": "prettier --check .",
        "format:fix": "prettier --write ."
    },
    "devDependencies": {
        "@eslint/js": "catalog:",
        "globals": "catalog:",
        "husky": "^9.1.7",
        "lint-staged": "^16.4.0",
        "prettier": "catalog:"
    },
    "dependencies": {
        "@types/node": "catalog:"
    },
    "pnpm": {
        "overrides": {
            "http-errors": "^2.0.1",
            "statuses": "^2.0.2",
            "picomatch": "^4.0.4",
            "lodash": "^4.18.0",
            "lodash-es": "^4.18.0",
            "yaml": "^2.8.3",
            "path-to-regexp": "^8.4.2",
            "follow-redirects": "^1.16.0",
            "immutable": "^5.1.5",
            "conf>ajv": "^8.18.0"
        }
    }
}
