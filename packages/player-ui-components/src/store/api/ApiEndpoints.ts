/**
 * Cloud endpoint paths. Re-exported from `@ezplayer/ezplayer-core` so the renderer,
 * node main, and the cloud-poll worker all share one source.
 */
export { CLOUD_API_ENDPOINTS as API_ENDPOINTS } from '@ezplayer/ezplayer-core';
